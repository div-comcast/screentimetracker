import 'package:flutter/material.dart';
import 'frontend/app.dart';

/// App entry point — `flutter run` launches the frontend UI skeleton.
///
/// Backend wiring intentionally lives elsewhere and is not called here yet.
void main() {
  runApp(const ScreenTimeApp());
}
